
[project]
name = "openenv-support-triage"
version = "0.1.0"
description = "OpenEnv Support Triage Environment"
authors = [{name = "Palak Kasaudhan"}]
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.setuptools]
packages = ["env", "agent", "graders", "tasks"]